# Copiar a .env y llenar con los nombres reales que imprime `cdk deploy`
# (ApplicationsFunction/AuthFunction environment, o los outputs del stack).
USERS_TABLE=
APPLICATIONS_TABLE=
VIDEOS_BUCKET=
JWT_SECRET=dev-only-secret-change-me
CORS_ORIGIN=http://localhost:5173
PORT=3001
